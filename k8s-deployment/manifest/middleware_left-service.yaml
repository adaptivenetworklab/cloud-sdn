apiVersion: v1
kind: Service
metadata:
  name: middleware-left
spec:
  selector:
    app: middleware-left
  ports:
  - name: to-flowvisor
    protocol: TCP
    port: 10001
    targetPort: 10001
  - name : port-to-ryu_app
    protocol: TCP
    port: 8080
    targetPort: 8080